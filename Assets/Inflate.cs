using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Inflate : MonoBehaviour {

    public float m_displacement = 1f ;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {

        //transform.localScale = new Vector3(transform.localScale.x + m_displacement * Time.deltaTime, transform.localScale.y + m_displacement * Time.deltaTime, transform.localScale.z + m_displacement * Time.deltaTime);
		
	}
}
